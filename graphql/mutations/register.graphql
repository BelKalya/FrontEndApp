mutation Register($password: String!, $email: String!) {
  register(password: $password, email: $email) {
    user {
      email
      twitter
      instagram
      id
      facebook
      description
      contactName
      company
    }
    errors {
      field
      message
    }
  }
}
